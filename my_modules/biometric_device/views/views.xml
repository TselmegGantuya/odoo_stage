<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="biometric_device.list">
      <field name="name">biometric_device list</field>
      <field name="model">biometric_device.biometric_device</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="biometric_device.action_window">
      <field name="name">biometric_device window</field>
      <field name="res_model">biometric_device.biometric_device</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="biometric_device.action_server">
      <field name="name">biometric_device server</field>
      <field name="model_id" ref="model_biometric_device_biometric_device"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="biometric_device" id="biometric_device.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="biometric_device.menu_1" parent="biometric_device.menu_root"/>
    <menuitem name="Menu 2" id="biometric_device.menu_2" parent="biometric_device.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="biometric_device.menu_1_list" parent="biometric_device.menu_1"
              action="biometric_device.action_window"/>
    <menuitem name="Server to list" id="biometric_device" parent="biometric_device.menu_2"
              action="biometric_device.action_server"/>
    -->
  </data>
</odoo>